// Copia este archivo como env.js (en la raíz) y completa tus valores.
// Importante: restringe tu API Key por dominio (HTTP referrers) en Google Cloud.
// Este archivo NO debe subirse a git. Asegúrate que env.js está en .gitignore.

window.ENV = {
  GOOGLE_API_KEY: "TU_API_KEY_AQUI",
  PLACE_ID: "TU_PLACE_ID_AQUI"
};
